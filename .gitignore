target/
.mvn/wrapper/maven-wrapper.jar
*.class
*.log
.idea/
*.iml
.vscode/
.DS_Store
.env

# Real secret values for docker-compose's file-based secrets — only the *.example
# templates (same dev-only placeholders as before) are committed. See README.
secrets/*.txt
